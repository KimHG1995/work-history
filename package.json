{
  "name": "work-history",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:prepare": "node scripts/prepare-site.mjs",
    "docs:dev": "npm run docs:prepare && vitepress dev site",
    "docs:build": "npm run docs:prepare && vitepress build site",
    "docs:preview": "vitepress preview site --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "1.6.4",
    "mermaid": "^11.0.0"
  }
}
