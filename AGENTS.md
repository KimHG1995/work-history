# 이 저장소에서 작업할 때

이 저장소는 회사 프로젝트와 사이드 프로젝트의 작업 내용을 기록하는 곳입니다.

## 문서 구조

- 같은 플랫폼이나 프로젝트의 업무는 `projects/<묶음>/` 폴더에 모읍니다.
- 폴더의 `README.md`에는 짧은 개요와 작업 요약, 작업 파일 링크만 적습니다.
- 업무마다 `<작업>.md` 파일 하나를 만듭니다. 같은 목적의 커밋은 한 작업으로 묶습니다.
- 설명, 작업 이유, 역할 분담, 기술, 기간, 결과, 운영 상태, 순서도, 근거는 해당 작업 파일 한 곳에 적습니다.
- 구조와 커밋 기록을 별도 파일로 나누거나 상세 내용을 여러 문서에 반복하지 않습니다.
- 짧은 인턴 업무는 `experience.md`에 유지합니다.
- 새 묶음은 `templates/project/README.md`, 새 업무는 `templates/task.md`를 복사해 씁니다.

## 글쓰기

- 한국어로 짧고 쉽게 씁니다.
- 가운뎃점을 쓰지 않습니다. 항목은 쉼표나 줄바꿈으로 나눕니다.
- 어려운 말보다 일상에서 쓰는 말을 고릅니다.
- 기술 이름과 파일 이름은 원래 이름을 씁니다.
- 비공개 작업 저장소의 주소, 조직명과 저장소 이름은 공개 문서에 적지 않습니다. 분석에 제공된 저장소 링크도 공개 허용으로 해석하지 않습니다.
- 회사 이름이나 별칭은 적지 않습니다. 어떤 일을 하는 회사인지만 한 문장으로 적습니다.
- 프로젝트는 `회사`와 `사이드`로 구분합니다. 사이드 프로젝트의 회사 설명은 `해당 없음`으로 적습니다.
- 함께한 사람의 이름과 계정 이름은 적지 않습니다. 담당 역할과 함께한 일만 적습니다.
- 협업 내용은 `역할 분담` 항목에 본인과 다른 담당자가 맡은 일을 적습니다.
- 한 문장에는 한 가지 내용을 담습니다.
- 기술 목록은 `기술 스택`으로 표기합니다. 프론트엔드, 백엔드, 데이터베이스, 인프라 등 익숙한 기술 용어를 쓰되 불필요한 영어와 전문 용어는 줄입니다.
- 경력 본문에는 회사 경력에 포함하는 이유, 문서 읽는 방법, 작성 과정 설명을 넣지 않습니다. 같은 역할과 처리 방식은 반복하지 않습니다.
- 공개 문서에는 자료 출처나 대화 과정 같은 작성 경위와 형식적인 `기록 근거` 항목을 남기지 않습니다. 실제 작업과 결과를 적고, 확인되지 않은 내용은 해당 항목에 짧게 표시합니다.
- 작업마다 시작한 이유, 문제, 해결 방법, 결과, 남은 일을 적습니다.
- 업무 흐름은 해당 작업 파일 안에 Mermaid 순서도로 함께 적습니다.
- 순서도에는 확인한 단계만 넣습니다. 실행 조건과 실패 처리 등 모르는 부분은 `확인 필요`로 남깁니다.

## 상세 문서 공통 형식

- 기본 정보는 `항목 / 내용` 표로 작성하며 행은 `작업 기간`, `맡은 범위`, `개발 상태`, `운영 상태` 순서로 둡니다.
- 회사 설명과 전체 근무 기간은 근무 기록과 프로젝트 개요에 둡니다. 상세 문서에는 실제 작업 기간을 적습니다.
- 기술 스택은 `구분 / 기술` 두 열 표로 통일합니다. 기술 선택 이유와 자세한 용도는 본문에 적습니다.
- 기술 분류는 언어, 프론트엔드, 백엔드, 데이터베이스, 스토리지, 인프라와 배포 등을 사용하되 해당하는 항목만 둡니다.
- ORM과 검증 도구는 백엔드에 분류합니다. 다른 담당자가 사용한 기술은 따로 설명합니다.
- 공통 제목은 `기본 정보`, `왜 시작했는지`, `역할 분담`, `기술 스택`, `어떻게 해결했는지`, `결과와 현재 상태`를 사용합니다.
- 역할 분담은 추가할 협업 내용이 있을 때만 둡니다. 작업별 구조도, 비용 비교와 처리 흐름 제목은 내용에 맞게 유지합니다.
- 개발 배포일 등 추가 일정은 기본 정보 아래에 보존합니다. 운영 반영일은 운영 상태에 함께 적습니다.

## 사실 확인

- 프로젝트 전체 작업과 내가 한 작업을 나눠 적습니다.
- 프로젝트에서 쓰는 기술과 내가 직접 사용한 기술을 나눠 적습니다.
- 커밋 수만으로 기여도나 진행률을 정하지 않습니다.
- 커밋 날짜만으로 실제 참여 기간을 확정하지 않습니다.
- 함께한 사람의 역할은 확인한 내용만 적습니다.
- 개발 완료와 실제 서비스 반영을 나눠 적습니다.
- 확인하지 못한 내용은 `확인 필요`, 해당하지 않는 내용은 `해당 없음`으로 적습니다.
- 예시는 실제 기록과 구분합니다. 성과 수치나 작업 내용을 만들어 넣지 않습니다.

## 수정 후 확인

- 문서의 링크가 맞는지 확인합니다.
- 가운뎃점이 없는지 확인합니다.
- 함께한 사람의 이름이나 계정 이름을 적는 항목이 없는지 확인합니다.
- 회사 이름이나 별칭을 적는 항목이 없는지 확인합니다.
- 프로젝트 목록과 기간별 기록에 빠진 내용이 없는지 확인합니다.
- 사용자가 요청한 파일만 커밋에 넣습니다.
- 관련 변경은 작업 브랜치에서 커밋하고 PR로 검증한 뒤 `main`에 반영합니다. `main`에 직접 푸시하지 않습니다.
- 서로 다른 목적의 변경은 같은 브랜치에서 작업하더라도 커밋을 나눕니다.
- PR의 문서 검증과 GitHub Pages 배포 결과를 확인합니다.

## 목록과 개요 표

- 전체 작업 목록과 프로젝트 개요의 작업 목록은 `작업 / 요약` 두 열 표를 사용합니다.
- 같은 작업은 두 목록에서 같은 요약을 사용합니다. 링크 이름은 `프로젝트 개요`로 통일합니다.
- 요약은 직접 한 일을 한 문장으로 적습니다. 상태, 배포 날짜와 긴 배경 설명은 목록에 반복하지 않습니다.
- 개발과 운영 상태, 배포 날짜는 상세 문서의 기본 정보에 둡니다. 결과 본문에는 실제 달라진 점을 적습니다.
- `왜 시작했는지`는 문제와 바꾸려던 이유만 짧게 적습니다. 국가, 기존 기술과 발송 종류 등은 이해에 꼭 필요한 경우에만 설명합니다.
