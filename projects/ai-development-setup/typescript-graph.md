# TypeScript 코드 분석용 MCP 연결

[AI 개발 환경 작업 목록](README.md)으로 돌아갑니다.

TypeScript 코드의 호출 관계와 구조를 AI가 조회할 수 있도록 외부 MCP 도구를 설치하고 프로젝트별로 연결하는 환경을 구성했습니다.

[코드 저장소](https://github.com/KimHG1995/ts-graph-tools)

## 기본 정보

| 항목 | 내용 |
| --- | --- |
| 작업 기간 | 2026-07-22, 약 1일. 커밋 기록 기준 |
| 맡은 범위 | 외부 분석 도구 실행 환경, 프로젝트별 MCP 등록 스크립트와 사용 안내 작성 |
| 개발 상태 | 연결 스크립트와 실행 환경 구성 |
| 운영 상태 | 로컬 프로젝트에 등록해 사용하는 개발 도구로 구성 |


## 왜 시작했는지

AI가 함수의 호출 관계와 수정 영향 범위를 확인할 때, 파일을 하나씩 찾는 작업을 보완하고자 했습니다.
기존 코드 검색과 TypeScript 검사도 계속 사용할 수 있지만, 코드 관계를 MCP로 조회할 수 있도록 별도 분석 도구를 연결했습니다.

분석 패키지를 각 프로젝트의 개발 의존성으로 설치하는 방식도 가능합니다.
다만 개인용 AI 분석 도구를 추가할 때마다 서비스의 `package.json`과 잠금 파일을 수정하고, 프로젝트별로 도구 버전을 관리하는 일을 줄이고자 했습니다.

선택한 도구는 TypeScript 7 네이티브 검사기를 사용합니다.
서비스의 빌드와 테스트는 기존 TypeScript 버전을 유지하고, AI 분석만 별도로 설치한 검사기를 사용하도록 나눴습니다.
기존 버전으로 개발할 수 없어서 올리는 작업이 아니라, 서비스 개발 환경과 분석 도구의 설치 및 업데이트를 따로 관리하려는 선택입니다.

한 번 설치한 분석 환경을 여러 프로젝트에서 재사용하고, 대상 프로젝트에는 로컬 MCP 연결만 등록하도록 구성했습니다.

## 기술 스택

| 구분 | 기술 |
| --- | --- |
| 등록 스크립트 | Bash |
| 실행 환경 | Node.js, npm |
| AI 도구 연결 | MCP, Claude Code CLI |
| 코드 분석 | @ttsc/graph, ttsc, TypeScript 네이티브 검사기 |

## 어떻게 해결했는지

### 분석 도구와 대상 프로젝트 분리

분석 패키지와 실행 파일은 별도 도구 저장소에 설치했습니다.
대상 프로젝트의 패키지와 잠금 파일을 수정하지 않고 로컬 범위의 MCP 설정으로 연결했습니다.
분석할 프로젝트를 작업 디렉터리로 사용하고, 그래프 생성기와 TypeScript 검사기는 별도 설치 경로를 지정했습니다.

### 플랫폼별 등록 자동화

등록 스크립트에서 macOS와 Linux, ARM과 x64 환경을 구분했습니다.
필요한 실행 파일이 있는지 확인한 뒤 프로젝트별 MCP 등록 명령을 실행하도록 구성했습니다.
함께 사용하는 분석 패키지의 버전을 고정해 실행 환경을 맞췄습니다.

### 코드 관계 조회

외부 도구가 제공하는 함수 위치, 호출 관계, 타입 정보와 진단을 AI에서 조회할 수 있도록 연결했습니다.
그래프 엔진을 직접 개발한 작업이 아니라 기존 도구의 설치와 연결을 재사용 가능하게 만든 작업입니다.
린트나 프로젝트 자체 빌드는 별도로 유지했습니다.
분석용 검사기와 프로젝트의 TypeScript 버전이 다르면 진단 결과가 달라질 수 있으므로, 최종 검증은 프로젝트의 기존 빌드와 테스트를 기준으로 합니다.

## 처리 흐름

```mermaid
flowchart LR
    agent["대상 프로젝트의 AI 코딩 도구"] --> mcp["로컬 MCP 등록"]
    mcp --> host["별도로 설치한 분석 서버"]
    host --> analyzer["그래프 생성기와 TypeScript 검사기"]
    analyzer --> source["대상 프로젝트 코드와 tsconfig 분석"]
    source --> result["심볼, 호출 관계와 타입 정보 반환"]
```

## 결과와 현재 상태

분석용 의존성과 프로젝트의 기존 빌드 환경을 분리했습니다.
등록 스크립트로 여러 프로젝트에 같은 분석 환경을 연결할 수 있도록 구성했습니다.
호출 수와 토큰 절감 효과는 별도로 측정하지 않았습니다.
